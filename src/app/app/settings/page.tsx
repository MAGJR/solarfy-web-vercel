// This page now serves as the entry point and redirects to the main settings layout
// The actual content is handled by the layout.tsx and components/

export default function SettingsPage() {
  // The layout will handle the content rendering
  return null
}